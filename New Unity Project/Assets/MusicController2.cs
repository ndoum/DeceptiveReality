using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MusicController2 : MonoBehaviour {
    /*
    public AudioSource secondSource;

    public WaterTime water;

    bool musicFirstToggle = true;

    AudioSource secondAudio = secondSource.GetComponent<AudioSource>();


	// Update is called once per frame
	void Update () {
        if (water.WaterIsRising == true && musicFirstToggle == true) {
            //turn off this component's audio source
            this.Stop();

            //Play audio
            secondAudio.Start();
            //Ensure audio doesn't play more than once
            musicFirstToggle = false;
        }


	}
    */
}
